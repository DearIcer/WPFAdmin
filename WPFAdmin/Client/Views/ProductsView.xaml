<UserControl x:Class="Client.Views.ProductsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- 标题和工具栏 -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0"
                       Text="商品管理"
                       Style="{StaticResource MaterialDesignHeadline4TextBlock}"
                       Margin="0,0,0,16" />

            <StackPanel Grid.Column="1"
                        Orientation="Horizontal">
                <Button Style="{StaticResource MaterialDesignFloatingActionMiniButton}"
                        ToolTip="添加商品">
                    <materialDesign:PackIcon Kind="Plus" />
                </Button>
            </StackPanel>
        </Grid>

        <!-- 搜索和过滤区域 -->
        <materialDesign:Card Grid.Row="1" Margin="0,0,0,16">
            <StackPanel Margin="16">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <TextBox Grid.Column="0"
                             materialDesign:HintAssist.Hint="商品名称"
                             Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                             Margin="0,0,8,0" />

                    <ComboBox Grid.Column="1"
                              materialDesign:HintAssist.Hint="商品分类"
                              Style="{StaticResource MaterialDesignFloatingHintComboBox}"
                              Margin="0,0,8,0">
                        <ComboBoxItem>数码产品</ComboBoxItem>
                        <ComboBoxItem>服装</ComboBoxItem>
                        <ComboBoxItem>食品</ComboBoxItem>
                        <ComboBoxItem>家具</ComboBoxItem>
                    </ComboBox>

                    <ComboBox Grid.Column="2"
                              materialDesign:HintAssist.Hint="状态"
                              Style="{StaticResource MaterialDesignFloatingHintComboBox}"
                              Margin="0,0,8,0">
                        <ComboBoxItem>全部</ComboBoxItem>
                        <ComboBoxItem>上架</ComboBoxItem>
                        <ComboBoxItem>下架</ComboBoxItem>
                    </ComboBox>

                    <Button Grid.Column="3"
                            Content="搜索"
                            Style="{StaticResource MaterialDesignRaisedButton}"
                            VerticalAlignment="Bottom" />
                </Grid>
            </StackPanel>
        </materialDesign:Card>

        <!-- 商品列表 -->
        <materialDesign:Card Grid.Row="2">
            <DataGrid ItemsSource="{Binding Products}"
                      CanUserAddRows="False"
                      IsReadOnly="True"
                      AutoGenerateColumns="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="商品ID" Binding="{Binding Id}" Width="100" />
                    <DataGridTextColumn Header="商品名称" Binding="{Binding Name}" Width="*" />
                    <DataGridTextColumn Header="分类" Binding="{Binding Category}" Width="100" />
                    <DataGridTextColumn Header="价格" Binding="{Binding Price}" Width="100" />
                    <DataGridTextColumn Header="库存" Binding="{Binding Stock}" Width="100" />
                    <DataGridTextColumn Header="状态" Binding="{Binding Status}" Width="100" />
                    <DataGridTemplateColumn Header="操作" Width="150">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Content="编辑"
                                            Style="{StaticResource MaterialDesignFlatButton}"
                                            Padding="8,4"
                                            FontSize="10" />
                                    <Button Content="删除"
                                            Style="{StaticResource MaterialDesignFlatButton}"
                                            Padding="8,4"
                                            FontSize="10"
                                            Margin="4,0,0,0" />
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </materialDesign:Card>
    </Grid>
</UserControl>