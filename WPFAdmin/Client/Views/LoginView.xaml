<UserControl x:Class="Client.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="1000">
    <Grid Background="White">
        <Border CornerRadius="10" Background="White"
                HorizontalAlignment="Center" VerticalAlignment="Center"
                Padding="40" MinWidth="400">
            <Border.Effect>
                <DropShadowEffect ShadowDepth="2" BlurRadius="10" Opacity="0.3" />
            </Border.Effect>

            <StackPanel>
                <TextBlock Text="管理员登录" FontSize="24" FontWeight="Bold"
                           HorizontalAlignment="Center" Margin="0,0,0,30"
                           Foreground="Black" />

                <TextBox x:Name="UsernameTextBox"
                         materialDesign:HintAssist.Hint="用户名"
                         Style="{StaticResource MaterialDesignOutlinedTextBox}"
                         FontSize="14" Padding="10" Margin="0,0,0,20" />

                <PasswordBox x:Name="PasswordBox"
                             materialDesign:HintAssist.Hint="密码"
                             Style="{StaticResource MaterialDesignOutlinedPasswordBox}"
                             FontSize="14" Padding="10" Margin="0,0,0,30" />

                <Button x:Name="LoginButton" Content="登录"
                        Style="{StaticResource MaterialDesignRaisedButton}"
                        Background="#FF4081" BorderBrush="#FF4081"
                        Foreground="White" FontSize="16"
                        Padding="20,0" Margin="0,0,0,20"
                        Click="LoginButton_Click" />

                <TextBlock x:Name="ErrorTextBlock"
                           Text=""
                           Foreground="Red"
                           HorizontalAlignment="Center"
                           TextWrapping="Wrap"
                           Margin="0,0,0,10" />

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <CheckBox Content="记住我" Margin="0,0,20,0" />
                    <TextBlock Text="忘记密码?" Foreground="#FF4081" Cursor="Hand" Margin="0,10,0,0" />
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>